# SafeHarbor Server Environment Variables

# Secret key for signing JWT tokens — use a long, random string in production
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Port the server will listen on
PORT=5001

# Path to the SQLite database file (relative to project root or absolute)
DB_PATH=./safeharbor.db
